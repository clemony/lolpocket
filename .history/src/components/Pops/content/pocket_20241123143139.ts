import type {menuGroup, menuItem} from 'types'

export const menu = [
    {
    title: 
    type: checkbox
    link?: string
    icon?: string
    iconFalse?: string
    shortcut?: string
    subMenu?: Array<menuItem>
    }
]