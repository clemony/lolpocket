// server/db/client.ts
/* import type { D1Database } from '@nuxthub/core'
import { championItems, championRunes, champions, championSummoners } from 'db'
import { drizzle } from 'drizzle-orm/d1'

// Tell TypeScript the D1 binding exists at runtime
declare const D1: D1Database

// Combine your schemas into a single object
const schema = {
  championRunes,
  championItems,
  championSummoners,
  champions,
} as const

// Create the db client
export const db = []// drizzle(D1, { schema })

// Optionally, export table names for autocomplete in queries
export const tables = schema
 */
