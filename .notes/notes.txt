//NOTE

live refresh together from db ?

 transition-colors duration

puuid == sole summoner id and used for navigation? fetch summoner data from riot and use for url then pass puuid as prop



const resolve = await ss().resolveSummoner({ puuid: as().account.puuid })
const update = {
  puuid: resolve.puuid,
  name: resolve.name,
  tag: resolve.tag,
  region: resolve.region,
  profileIcon: resolve.profileIcon,
}
Object.assign(as().account.riot, update)