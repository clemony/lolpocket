export async function useNavigateToSummoner(puuid: string) {
  // const summoner = await useResolveSummoner({ puuid })
  // navigateTo(`/summoner/${summoner.region}/${summoner.name}_${summoner.tag}`)
}