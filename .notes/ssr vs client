🧭 Nuxt SSR vs Client-only Decision Tree

1. Does the page need to be indexed by search engines or have sharable metadata?
	•	Yes → Use SSR/SSG.
	•	No → Go to step 2.

⸻

2. Does the page need server-side data at request time (API, DB, auth)?
	•	Yes → Use SSR.
	•	No → Go to step 3.

⸻

3. Is the content meaningful without interactivity (e.g. article, product info, docs)?
	•	Yes → Use SSR/SSG so users and crawlers see it immediately.
	•	No → Go to step 4.

⸻

4. Is the page mostly user-driven UI (dashboards, widgets, pickers, settings)?
	•	Yes → Use Client-only (or keep SSR but wrap those components in <ClientOnly>).
	•	No → Default to SSR.

⸻

5. Does it rely on browser-only APIs (window, localStorage, media devices)?
	•	Yes → Component(s) must be Client-only, even if the page is SSR.

⸻

✨ Key Takeaways
	•	Default to SSR/SSG → It’s safe, and you can mix in <ClientOnly> as needed.
	•	Only go client-only when SSR is wasted (no SEO, no server data, UI is entirely interactive).
	•	Landing pages: SSR the marketing copy, <ClientOnly> the fancy widgets.
	•	Apps/dashboards: SSR the shell (nav, layout) → client-only for the guts if they don’t matter server-side.