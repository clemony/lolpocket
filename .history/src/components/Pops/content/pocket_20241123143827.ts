import type {menu, menuGroup, menuItem} from 'types'

export const pocketMenu: menu = [
    {
    title: 
    type: checkbox
    link?: string
    icon?: string
    iconFalse?: string
    shortcut?: string
    subMenu?: Array<menuItem>
    }
]